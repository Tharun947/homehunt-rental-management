import { TenantApplicationsPage } from './tenant-applications';

describe('TenantApplicationsPage', () => {
  it('should be defined', () => {
    expect(TenantApplicationsPage).toBeTruthy();
  });
});
